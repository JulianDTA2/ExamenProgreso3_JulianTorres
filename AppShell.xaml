<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ExamenProgreso3_JulianTorres.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ExamenProgreso3_JulianTorres"
    xmlns:views="clr-namespace:ExamenProgreso3_JulianTorres.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="ExamenProgreso3_JulianTorres">

    <TabBar>
        <ShellContent Title="Nuevo" Icon="add.png"
                      ContentTemplate="{DataTemplate views:NuevoClientePage}" />

        <ShellContent Title="Clientes" Icon="list.png"
                      ContentTemplate="{DataTemplate views:ListaClientesPage}" />

        <ShellContent Title="Logs" Icon="file.png"
                      ContentTemplate="{DataTemplate views:LogsPage}" />
    </TabBar>

</Shell>
